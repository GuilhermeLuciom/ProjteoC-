#include <iostream>
using namespace std;

int main( )
{
    float ms;
    cout<<"Informe a velocidade m/s: ";
    cin>>ms;
    cout<<" A veloidade em km/h: "<<(ms*3.6);
    
    system("PAUSE");
    return (0);
}